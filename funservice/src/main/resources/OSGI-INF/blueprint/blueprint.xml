<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd">
    <!--xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"-->

    <!-- It gets a little magical with blueprint. First, we instantiate a bean. It is a singleton. We
    can reference it by the ID "fun". The bean is the service implementor and its class is "Fun" -->
    <bean id="fun" class="funservice.Fun"/>

    <!-- More magic. Declare a service. It can be referenced by its ID, "funService". The service's
    API is defined by the interface "FunService". -->
    <service id="funService" ref="fun" interface="funservice.FunService"/>

</blueprint>